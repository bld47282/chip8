void draw(chip8 * ch8) {
	for (int x = 0; x < 64; ++x) {
		for (int y = 0; y < 32; ++y) {
			if (ch8->graphics_buffer[x][y] = 0xFF) {
				printf("X");
			} else {
				printf(" ");
			}
		}
		printf("\n");
	}
}
